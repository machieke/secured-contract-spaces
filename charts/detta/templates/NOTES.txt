DeTTa has been installed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

Cluster topology:
  Validators: {{ .Values.validators.count }}
  RPC service: {{ include "detta.rpcName" . }}:{{ .Values.rpc.port }}

For k3s/local access:
  kubectl -n {{ .Release.Namespace }} rollout status statefulset/{{ include "detta.rpcName" . }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "detta.rpcName" . }} 8080:{{ .Values.rpc.port }}
  detta-client state-root --rpc 127.0.0.1:8080

Run the chart test:
  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}
